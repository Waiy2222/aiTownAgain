# 队员D 测试日志

> 对应计划书 [PLAN_队员D.md](PLAN_队员D.md) 第七章测试规范
> 以时间流记录，每次测试一条，不论成功失败

---

## 测试环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows 11 |
| 浏览器 | Chromium / Edge |
| Node 版本 | v22.22.2 |
| Convex | 本地开发模式 |
| 世界 ID | m171cf6rb4rqpn83d69etmazed874gqs |

---

## 测试记录

---

### [T01] 2026-05-__ __:__ — index.html 中文化

**测试方案**：打开页面 → 检查标签页标题 + Elements 面板 html lang + meta content

**测试数据**：
```
浏览器标签页标题：____
<html lang=""> 属性值：____
<meta name="description" content=""> 值：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```
（通过则写"各项均符合预期"，失败则写具体哪个不合预期 + 截图或日志片段）
```

**是否影响连通性**：□ 是 / □ 否（独立文件修改，不影响其他功能）

---

### [T02] 2026-05-__ __:__ — MusicButton 中文化

**测试方案**：页面加载 → 检查按钮文字 → hover 检查 title tooltip → 点击切换 → 检查文字变化

**测试数据**：
```
初始按钮文字：____
hover title tooltip：____
点击后按钮文字：____
再次点击后按钮文字：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（独立 UI 修改）

---

### [T03] 2026-05-__ __:__ — InteractButton 中文化

**测试方案**：页面加载 → 检查按钮文字 → 点击 → 检查按钮文字变化 → F12 Console 查看日志

**测试数据**：
```
初始按钮文字：____
点击后 Console 日志内容：____
点击后按钮文字：____
再次点击后 Console 日志内容：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（独立 UI 修改）

---

### [T04] 2026-05-__ __:__ — ACTIVITIES 中文化

**测试方案**：观察地图上 NPC 头顶的活动描述文字 → 等待不同 NPC 做不同活动

**测试数据**：
```
NPC 1 活动描述：____
NPC 2 活动描述：____
NPC 3 活动描述：____
所有描述是否中文：□ 是 / □ 否
emoji 是否保持不变：□ 是 / □ 否
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（Convex 后端数据修改）

---

### [T05] 2026-05-__ __:__ — PlayerDetails 验证

**测试方案**：点击任意 NPC → 检查右侧面板所有可见文字 → 检查 Console 日志

**测试数据**：
```
面板标题（NPC 名称）：____
状态文字：____
按钮文字（如有）：____
所有文字是否中文：□ 是 / □ 否
Console 日志内容：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（已中文，仅验证）

---

### [T06] 2026-05-__ __:__ — Messages 验证

**测试方案**：等待两个 NPC 对话 → 检查消息面板 → 确认 uppercase 样式已移除

**测试数据**：
```
加入提示文字：____
离开提示文字：____
作者名样式是否有 uppercase：□ 是 / □ 否（应当为否）
正在输入提示：____
时间格式：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（样式修改，不影响消息功能）

---

### [T07] 2026-05-__ __:__ — FreezeButton 验证

**测试方案**：hover 冻结按钮 → 检查 tooltip → 点击 → 检查 Console

**测试数据**：
```
hover tooltip 文字：____
点击后 Console 日志：____
按钮文字：____
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（已中文，仅验证）

---

### [T08] 2026-05-__ __:__ — App.tsx 验证

**测试方案**：页面加载 → 检查主标题/副标题 → 点帮助按钮 → 检查弹窗内容

**测试数据**：
```
主标题：____
副标题：____
帮助弹窗标题：____
帮助弹窗正文是否全中文：□ 是 / □ 否
帮助弹窗中有无英文单词：□ 有 / □ 无（应当无）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（已中文，仅验证）

---

### [T09] 2026-05-__ __:__ — conversation.ts 日志中文化

**测试方案**：等待 NPC 自然触发对话 → F12 Console → 过滤日志 → 与下面核对

**测试数据**：
```
创建对话日志：____（预期："正在创建对话：xxx"）
发起对话日志：____（预期："正在发起对话：xxx yyy"）
对话开始日志：____（预期："对话开始：xxx 与 yyy"）
手动撮合日志：____（预期："手动撮合：xxx 与 yyy"）
参与者异常日志：____（预期："对话 xxx 参与者数量异常：x" 或 未出现）
是否有英文开头的 console.log：□ 有 / □ 无（应当无）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（日志修改，不影响功能）

---

### [T10] 2026-05-__ __:__ — agent.ts 日志中文化

**测试方案**：观察 Console 中 Agent 相关日志 → 与下面核对

**测试数据**：
```
操作超时：____（预期："操作超时：{...}" 或 未出现）
记忆对话：____（预期："Agent xxx 正在记忆对话" 或 未出现）
接受邀请：____（预期："Agent xxx 接受了 xxx 的邀请" 或 未出现）
拒绝邀请：____（预期："Agent xxx 拒绝了 xxx 的邀请" 或 未出现）
正在走向：____（预期："Agent xxx 正在走向 xxx..." 或 未出现）
发起对话：____（预期："xxx 正在发起与 xxx 的对话" 或 未出现）
离开对话：____（预期："xxx 正在离开与 xxx 的对话" 或 未出现）
继续对话：____（预期："xxx 正在继续与 xxx 的对话" 或 未出现）
放弃邀请：____（预期："放弃等待 xxx 的邀请回应" 或 未出现）
是否有英文开头的 console.log：□ 有 / □ 无（应当无）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（日志修改，不影响功能）

---

### [T11] 2026-05-__ __:__ — player.ts 日志中文化

**测试方案**：观察 Console → 寻找"寻路超时"日志（需 NPC 寻路卡住场景）

**测试数据**：
```
寻路超时日志：____（预期："寻路超时：xxx" 或 未出现）
原英文 "Timing out pathfinding" 是否出现：□ 是 / □ 否（应当否）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（日志修改，不影响功能）

---

### [T12] 2026-05-__ __:__ — PixiGame 日志中文化

**测试方案**：拖拽地图（触发 drag 跳过） → 点击地图（触发 moveTo） → 检查 Console

**测试数据**：
```
拖拽导航日志：____（预期："跳过拖拽导航（xxpx）" 或 未出现）
移动日志：____（预期："正在移动到：{...}" 或 未出现）
是否有英文开头的 console.log：□ 有 / □ 无（应当无）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（日志修改，不影响功能）

---

### [T13] 2026-05-__ __:__ — conversation.ts prompt 验证

**测试方案**：逐行审查 `convex/agent/conversation.ts` 中的 prompt 模板字符串

**测试数据**：
```
"你是 ${player.name}，你刚开始与..."      → □ 中文
"请务必在你的问候语中包含..."               → □ 中文
"你目前正在与 ${otherPlayer.name} 对话。"  → □ 中文
"以下是你与 ${otherPlayer.name} 之间..."   → □ 中文
"你决定结束对话，并想礼貌地..."            → □ 中文
"关于你："                                 → □ 中文
"你此次对话的目标："                        → □ 中文
"关于 ${otherPlayer.name}："              → □ 中文
"你上次与 ${otherPlayer.name} 聊天是在..." → □ 中文
"以下是一些按相关性递减排序的相关记忆："    → □ 中文
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（已中文，仅验证）

---

### [T14] 2026-05-__ __:__ — modelName 字段

**测试方案**：启动旧世界 → 检查 Console 无序列化报错 → 确认 modelName 字段存在

**测试数据**：
```
世界启动是否报错：□ 是 / □ 否
Console 无 "modelName" 相关 Error：□ 是 / □ 否
新 Agent 的 modelName 默认值：____（预期：undefined 或 "默认"）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（可选字段，向后兼容）

---

### [T15] 2026-05-__ __:__ — forceConversation 后端

**测试方案**：手动撮合 2 个空闲 NPC → 撮合已在对话中的 NPC → 传输入同一 NPC → 传入不存在的 NPC

**测试数据**：
```
===== 场景 1：两个空闲 NPC =====
选中的 NPC A：____（playerId）
选中的 NPC B：____（playerId）
Network 标签中 forceConversation 返回：____
是否创建新对话：□ 是 / □ 否
两人是否走向对方：□ 是 / □ 否

===== 场景 2：其中一人已在对话中 =====
已有对话的 NPC：____
旧对话是否被 stop：□ 是 / □ 否
新对话是否创建：□ 是 / □ 否

===== 场景 3：两人都在对话中 =====
两个旧对话是否都被 stop：□ 是 / □ 否
新对话是否创建：□ 是 / □ 否

===== 场景 4：同一 NPC =====
传入同一 playerId 两次 → 返回错误信息：____（预期包含"不能撮合同一个NPC"）

===== 场景 5：不存在的 NPC =====
传入不存在的 playerId → 返回错误信息：____（预期包含"Player not found"）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（新增 handler，影响撮合功能链路的连通性）

**连通性回归**：
```
forceConversation → sendInput → conversation.ts handler → Conversation.start → world.conversations 新增
同时验证 startConversation / acceptInvite / rejectInvite / leave 四个已有 input 是否正常：
- startConversation：□ 正常
- acceptInvite：□ 正常
- rejectInvite：□ 正常
- leave：□ 正常
```

---

### [T16] 2026-05-__ __:__ — ForceConversation 前端

**测试方案**：页面加载 → 检查撮合面板 → 测试下拉框交互 → 点击发起对话 → 观察 UI 反馈

**测试数据**：
```
===== UI 渲染 =====
撮合面板标题：____（预期："手动撮合"）
下拉框 A 选项数：____（预期：与 NPC 数量一致）
下拉框 B 选项数：____（预期：与 NPC 数量一致）
人类玩家是否出现在下拉框中：□ 是 / □ 否（应当否）

===== 下拉框互斥 =====
下拉框 A 选 "Stella" → 下拉框 B 中 "Stella" 是否 disabled：□ 是 / □ 否
下拉框 B 选 "Kurt" → 下拉框 A 中 "Kurt" 是否 disabled：□ 是 / □ 否

===== 按钮状态 =====
未选 NPC 时按钮状态：□ disabled / □ 可点击（应当 disabled）
只选 A 未选 B 时按钮状态：□ disabled / □ 可点击（应当 disabled）
选满 2 个不同 NPC 后按钮状态：□ disabled / □ 可点击（应当可点击）

===== 对话中标记 =====
已在对话中的 NPC 下拉框显示：____（预期：显示"(对话中)"标记）

===== 发起撮合 =====
点击"发起对话" → toast 内容：____（预期："正在撮合「A名」与「B名」..."）
点击后下拉框是否重置为空：□ 是 / □ 否

===== 边界 =====
NPC < 2 个时面板显示：____（预期："需要至少 2 个 NPC 才能使用此功能"）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（新增前端组件，影响撮合前端→后端的完整链路）

**连通性回归**：
```
ForceConversation UI → useSendInput → forceConversation handler → toast → NPC 移动
同时验证点击 NPC → PlayerDetails 是否正常：□ 正常
```

---

### [T17] 2026-05-__ __:__ — NPCModelTooltip

**测试方案**：hover 不同 NPC → 检查 tooltip → 快速划过 → 边界测试 → 点击测试

**测试数据**：
```
===== 基本显示 =====
hover NPC 1 → tooltip 内容：____（预期："{名} · 默认"）
hover NPC 2 → tooltip 内容：____
hover NPC 3 → tooltip 内容：____
tooltip 是否在 200ms 内出现：□ 是 / □ 否

===== 消失行为 =====
mouseleave → tooltip 是否消失：□ 是 / □ 否
消失延迟约 ____ms（预期 ≤ 200ms）

===== 快速划过 =====
快速划过 5 个 NPC → 是否有残留 tooltip：□ 是 / □ 否（应当否）
最后显示的 tooltip 是否正确：□ 是 / □ 否

===== 边界翻转 =====
hover 右下角 NPC → tooltip 是否翻转到鼠标左侧/上方：□ 是 / □ 否
tooltip 是否完全在视口内：□ 是 / □ 否

===== 不拦截点击 =====
hover NPC 的状态下点击该 NPC → PlayerDetails 是否正常打开：□ 是 / □ 否
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（新增独立组件，不影响其他功能）

---

### [T18] 2026-05-__ __:__ — Player hover 事件

**测试方案**：hover NPC → Console 验证传递的坐标/名称 → 缩放平移后复测

**测试数据**：
```
hover NPC 传递的 screenX：____
hover NPC 传递的 screenY：____
hover NPC 传递的 name：____
hover NPC 传递的 model：____（预期："默认"）
名称是否与 PlayerDetails 中一致：□ 是 / □ 否

===== 缩放/平移后 =====
缩放地图后 hover 坐标是否正确：□ 是 / □ 否
平移地图后 hover 坐标是否正确：□ 是 / □ 否

===== click 不受影响 =====
hover 后 click NPC → PlayerDetails 显示：□ 正常 / □ 异常
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（hover 事件传递，与 click 独立）

**连通性回归**：
```
Player hover → handlePointerOver → onHover callback → Game.tsx setTooltip → NPCModelTooltip 渲染
完整链路是否正常：□ 是 / □ 否
```

---

### [T19] 2026-05-__ __:__ — Character pointer 事件

**测试方案**：hover 和 click NPC 精灵 → 确认 pointerover/pointerout 单独正常工作

**测试数据**：
```
pointerover 事件是否触发：□ 是 / □ 否
pointerout 事件是否触发：□ 是 / □ 否
pointerdown（click）是否仍然正常：□ 是 / □ 否
三个事件之间是否相互干扰：□ 是 / □ 否（应当否）
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（新增事件属性，原有 pointerdown 不变）

---

### [T20] 2026-05-__ __:__ — Game.tsx 集成

**测试方案**：页面加载 → 检查右侧栏布局 → 撮合面板是否渲染 → tooltip 功能完整

**测试数据**：
```
右侧栏是否包含 PlayerDetails：□ 是 / □ 否
右侧栏是否包含 ForceConversation：□ 是 / □ 否
分隔线是否显示在两者之间：□ 是 / □ 否
右侧栏滚动是否正常：□ 是 / □ 否

worldId/engineId 存在时面板是否正常渲染：□ 是 / □ 否
worldId/engineId 为空时页面是否崩溃：□ 是 / □ 否（应当否）

===== tooltip 状态 =====
hover NPC → Game 层 tooltip state 是否更新：□ 是 / □ 否
mouseleave → tooltip state 是否设为 null：□ 是 / □ 否
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（集成点，影响撮合+tooltip两条链路）

**连通性回归**：
```
完整 UI 链路：
  ForceConversation → useSendInput → forceConversation → 地图 NPC 移动 → 对话消息
  Player hover → PixiGame onHover → Game tooltip state → NPCModelTooltip 渲染 → mouseleave 消失
两条链路独立运行，互不干扰：
  - 撮合时 hover：□ 正常
  - hover 时撮合：□ 正常
同时验证已有功能：
  - 点击 NPC → PlayerDetails：□ 正常
  - 地图平移/缩放：□ 正常
  - 冻结/解冻：□ 正常
```

---

### [T21] 2026-05-__ __:__ — PixiGame 透传

**测试方案**：验证 onHover/onHoverEnd 从 Game.tsx 正确经过 PixiGame 传递到 Player

**测试数据**：
```
PixiGame 接收 onHover prop：□ 是 / □ 否
PixiGame 接收 onHoverEnd prop：□ 是 / □ 否
每个 Player 渲染时是否收到 onHover prop：□ 是 / □ 否
每个 Player 渲染时是否收到 onHoverEnd prop：□ 是 / □ 否
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

**是否影响连通性**：□ 是 / □ 否（纯透传，无逻辑）

---

### [T22] 2026-05-__ __:__ — 全量回归测试

**测试方案**：按计划书 7.5 回归检查清单逐项验证，全部勾选才算通过

**测试数据**：
```
回归检查清单
============

□  1. 页面标题 "AI 小镇"
□  2. 帮助弹窗全中文
□  3. MusicButton "音乐" + hover tooltip 中文
□  4. InteractButton "互动"
□  5. FreezeButton "冻结"
□  6. 控制台日志全中文，无英文 console.log
□  7. NPC 活动描述中文
□  8. NPC 对话消息中文
□  9. hover NPC → tooltip "{名} · 默认"
□ 10. mouseleave → tooltip 200ms 消失
□ 11. 撮合 2 个空闲 NPC → toast + 走向对方 + 对话
□ 12. 撮合 1 个已在对话中的 NPC → 旧对话结束 + 新对话创建
□ 13. 撮合 2 个都在对话中的 NPC → 两个旧对话结束 + 新对话创建
□ 14. 连续撮合 10 次 → 全部成功无报错
□ 15. 快速 hover 10 个 NPC → 无残留 tooltip
□ 16. 点击 NPC → PlayerDetails
□ 17. 地图平移/缩放
□ 18. 冻结/解冻
□ 19. TypeScript 编译 → npx tsc --noEmit 零错误
□ 20. Vite 编译 → 浏览器无白屏无 JS 报错
□ 21. Convex 后端 → 世界状态 running
□ 22. 向后兼容 → 旧世界启动不报错

勾选统计：__ / 22
```

**测试结果**：□ 通过 / □ 失败

**原因分析**：
```

```

---

## 缺陷汇总

| 编号 | 发现节点 | 严重级别 | 描述 | 状态 |
|------|---------|---------|------|------|
| — | — | — | — | — |

---

## 测试总结

```
总测试节点数：22
通过数：____
失败数：____
跳过数：____
通过率：____%

开始时间：2026-05-__ __:__
结束时间：2026-05-__ __:__
总耗时：____
```
